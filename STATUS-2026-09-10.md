# Vekst — project status, 2026-09-10

Prepared from the repository at `origin/main` e3d7b2d, and from the branches
`origin/implement-classification-taxonomy` 43de3af and
`origin/add-tenancy-and-rls` 1feb32e.

Supersedes `STATUS-2026-09-06.md`.

---

## Verdict

**2026-10-01 is still not credible. But the gap is now approximately 2x, not 4x.**

Four of the eighteen Demo changes are complete. A fifth is complete on a branch
and is not merged. Fifteen working days remain.

The last report said the schedule was impossible for two reasons: one person did
the work, and both blocking inputs were absent. Both statements are now false.
A second developer is real. D-1 is closed. D-2 is one seventh closed.

The new risk is different, and it is worse than it looks:

**Track A has 10.5 planned days of work, no change in flight, and no proposal.**

---

## 1. What changed since 2026-09-06

| Item | 2026-09-06 | 2026-09-10 |
| --- | --- | --- |
| Changes complete | 2 of 18 | 4 of 18, plus 1 on a branch |
| Developers, measured from git | 1 | 2 [Certain] |
| D-1, the category list | absent | **closed** |
| D-2, the seven export files | absent | 1 bank of 7 files |
| Changes in flight | none | 3 proposed, 1 at 70% |
| Provisioning change | no owner | no owner |

### 1.1 Complete and archived

| Change | Budget | State |
| --- | --- | --- |
| 0.1 `bootstrap-monorepo` | 4 → 9 | Archived. Four tasks stay open (see §5) |
| 0.2 `add-postgres-and-migrations` | 2 → 3.75 | Archived. Task 7.5 is now closed |
| 1.1 `add-tenancy-and-rls` | 3 | Archived 2026-09-08. 50 of 50 tasks |
| 1.2 `add-identity` | 1.5 | Archived 2026-09-07. 55 of 55 tasks |

### 1.2 D-1 is closed, and the work behind it is good

`eval/` holds an offline toolkit that builds the taxonomy from the founder's
files and then measures it. The output is committed:

- 101 categories in `eval/out/taxonomy.csv`, with `seed_categories.sql`.
- 71 country rules in `eval/out/rules.json`, with `seed_rules.sql`.
- Measured against **4,508 real transactions**, three countries, 2023-2024.

| Market | Rules | Rows classified | Accuracy |
| --- | --- | --- | --- |
| Belarus, Priorbank | 41 | 87.5% | 94.8% |
| Kazakhstan | 21 | 86.1% | 92.4% |
| Poland, PKO BP | 9 | 38.6% | 99.6% |

No rule names the customer it was measured on. This is the most valuable thing
built since the skeleton. It converts the classification engine from a guess
into a measured component. [Certain — `eval/README.md`, and `out/` is committed]

### 1.3 Track A moved, but not through a change

`core/internal/ingest` now holds a parser registry, charset decoding, number
locale detection, and a Priorbank parser with tests. Four redacted CSV files are
in `core/testdata/priorbank-by`.

This is part of change 2.2. **Change 2.2 was never proposed.** The code arrived
inside PR #4, which is the taxonomy PR. [Certain — `git show e3d7b2d --name-only`]

---

## 2. The uncomfortable part

### 2.1 Two of the last four working days produced no work

All of Artsemi's eleven commits carry the date 2026-09-08. All of your commits
on that branch carry 2026-09-07 or 2026-09-08. On 2026-09-09 and 2026-09-10 the
repository received one thing: the merge of PR #4. [Certain — `git log --all --since=2026-09-06`]

The velocity that makes this report better than the last one comes from two
days. Do not plan on it until a third and a fourth day agree with it.

### 2.2 Track A is not staffed

The plan puts one developer on ingest and one on meaning. That is not what
happened. Both developers worked on Track B and on the web.

| Directory | Contents |
| --- | --- |
| `core/internal/ingest` | parser registry and one bank. Part of 2.2 |
| `core/internal/dedup` | `doc.go` only |
| `core/internal/report` | `doc.go` only |

Changes 2.1, 2.3, 2.4, 2.5 and 2.6 are not proposed and not started. That is
**10.5 planned days**, and it is the half of the Demo that you cannot fake. A
Demo without ingest is a screenshot, and `IMPLEMENTATION_PLAN.md` §3 forbids
that.

### 2.3 D-2 is one seventh closed, and the rest of the files already exist

`core/testdata` holds four CSV files from one Belarusian bank. The plan asks for
seven files: five bank and two 1C.

The Kazakh and Polish statements **already exist** on disk. `eval/README.md`
says the harness reads them from `../docCl`. They were measured. They were not
redacted into fixtures.

That is the cheapest task on this list. Artsemi redacted the Priorbank files on
2026-09-08 in one commit. Repeat it twice.

### 2.4 The web change carries scope the Demo does not need

`add-web-experience` is at **69 of 98 tasks**. Of the 29 open tasks, **18 are
ECharts work** — Sankey, small multiples, diverging columns, crosshair tooltips.

The Demo definition of done is one Management P&L with drill-down. It is a
table. No chart in §8 is named in the Demo scope of `IMPLEMENTATION_PLAN.md` §3.

The change says so itself: it bundles 5.1a, 5.1b, 5.2a, 5.2b, 5.2c and a landing
page against the rule in `CLAUDE.md`, by decision. §8 is a named seam. Cut it.

### 2.5 Change 3.1 is finished and not merged, and it blocks 3.2

`origin/implement-classification-taxonomy` holds migration 005, typed queries
over the taxonomy, and isolation tests. 25 of 26 tasks are done. One task is
open: task 0.4, a nullable `memberships.entity_id` column.

Change 3.2 `add-classification-engine` consumes this schema. It has 37 open
tasks and cannot start. Every day this branch waits is a day Track B waits.

### 2.6 The pull requests are too large, and review is now the slow step

- PR #5 carried change 1.1, change 1.2 and 69 web tasks in one squash.
- PR #4 carried 9,587 added lines. It waited two days for the merge.

`IMPLEMENTATION_PLAN.md` §2.1 says a branch older than two days is a merge
problem. `origin/add-tenancy-and-rls` carries five commits named `commit`,
`continue`, `fix`, `ci fix` and `sonar fix`.

### 2.7 The provisioning change still has no owner

This is the fourth report to say it. The cluster, DNS, TLS and backups belong to
no change and to no budget. D-6 names no host. Task 9.6 of the bootstrap change
is still open, and that task is the one that asks the founder.

Fifteen working days remain. A hosted Demo that nobody provisions is not a
hosted Demo.

---

## 3. The arithmetic, recomputed today

| Item | Value |
| --- | --- |
| Working days, 2026-09-11 → 2026-10-01 | **15** |
| Two developers, raw | 30 person-days |
| × 0.8 for real days | **24 usable person-days** |
| Capacity per working day, at the plan's 1.6x | 1.6 person-days |

Remaining Demo scope, in planned days:

| Block | Planned | Remaining |
| --- | --- | --- |
| Track A — 2.1 to 2.6 | 12 | **10.5** |
| Track B — 3.1 to 4.2 | 10.5 | **9.3** |
| Web — §9 only, if §8 is cut | 5 | **2** |
| Provisioning — unbudgeted | 0 | **2.5** |
| **Total** | | **≈ 24.3** |

24.3 planned days against 24 usable days. **The date fits only if every remaining
change lands exactly on budget.** That has not occurred once in this project.

Apply an overrun factor:

| Factor | Person-days | Working days at 1.6x | Date |
| --- | --- | --- | --- |
| 1.0x (perfect) | 24.3 | 15 | 2026-10-01 |
| 1.5x | 36.5 | 23 | ≈ 2026-10-13 |
| 2.12x (measured, changes 0.1 and 0.2) | 51.5 | 32 | ≈ 2026-10-26 |

**The honest Demo date is mid to late October.** If §8 charts stay in the change,
add two more days to every row.

---

## 4. Code health — still good

| Item | Count |
| --- | --- |
| Go files, excluding generated | 70 |
| Go test files | 31 |
| Web modules, excluding generated | 61 |
| Web test files | 14 |
| Migrations on `main` | 4, plus 005 on a branch |

The hard invariants hold where I can check them:

- `int64` minor units, with no-float guards in three languages.
- `organizations` → `entities` → `accounts`, with forced RLS and a non-owner app role.
- `deploy/db/rls-exempt-tables.txt` and the new `rls-shared-tenant-tables.txt`.
- Append-only classifications carrying the engine version fields.
- `pgxpool` imported by `core/internal/db` and `core/internal/jobs` only.
- Local `main` matches `origin/main`.

**Correction to an earlier draft of this report:** the working tree is **not** clean.
181 untracked files with a ` 2.` suffix are present — `jobs 2.go`, `identity 2.proto`,
`tenancy 2.sql`, `STATUS-2026-09-06 2.md` and so on. They are a file-sync duplication, not
work. They are invisible to `git status` at a glance because they sort beside their
originals, and `make gen` drift checks do not look at them. See §5.

**Not verified from here:** the CI result, the GitHub PR state, and `make ci`.
The desktop workspace has no Go, no Docker, no `buf` and no `gh`. [Stated, not proven]

---

## 5. Small items, cheap to correct

| Item | Why it matters |
| --- | --- |
| Four open tasks in the archived bootstrap change | 8.9 proves the CI gates work. 9.3 records the required checks. 9.6 asks the founder about the host. An archived change with open tasks hides them |
| Commit messages on `add-tenancy-and-rls` | `commit`, `continue`, `fix`, `ci fix`, `sonar fix` |
| Delete the merged branches | `add-postgres-and-migrations` and `add-tenancy-and-rls` are merged and still present |
| 181 untracked ` 2.` duplicate files | A file-sync artefact. `core/internal/jobs/jobs 2.go` compiles into the same package as `jobs.go` if anyone ever tracks it. Delete them, and add the pattern to `.gitignore` |
| `.github/CODEOWNERS` has no `/web` line | Task 9.8 of the web change. `/web` is now the largest directory with no owner |

---

## 6. Decisions that need an answer

| ID | Decision | Due | State |
| --- | --- | --- | --- |
| D-2 | The remaining export files | 27 Aug | **1 of 7.** The Kazakh and Polish files exist in `../docCl` |
| D-3 | The predefined output table structure | **15 Sep** | Unanswered. Three working days remain |
| D-6 | The hosting target | overdue | Unanswered five times. Working assumption: Hetzner EU with k3s |
| **New** | **Does L0.5 apply to bank rows?** | before 3.2 | `ARCHITECTURE.md` §4.1 forbids it. `eval/README.md` shows 16 Kazakh КНП pairs that classify with no exception. The measurement disagrees with the invariant |

The last one is new and it is not cosmetic. Kazakhstan reaches Belarus coverage
with half the rules **because** of a code on bank rows. Decide it before change
3.2 writes the layer, or you will write the layer twice.

---

## 7. What I would do, in order

**1. Propose and start Track A today.** Changes 2.1 `add-file-upload` and 2.5
`add-transaction-ledger` are blocked by nothing. This is the largest single risk
in the project and it is the one nobody is holding.

**2. Merge `implement-classification-taxonomy` today.** Close task 0.4 or record
it as deferred. Change 3.2 waits behind it, and 3.2 is 37 tasks.

**3. Cut §8 from `add-web-experience`.** Eighteen tasks. Move them to Product.
The Demo needs a table that opens, not a Sankey diagram.

**4. Redact the Kazakh and Polish statements into `core/testdata`.** The files
exist. The method exists. This closes D-2 to five of seven in one day.

**5. Give provisioning an owner and 2.5 days, or state today that the Demo runs
on a laptop.** Both answers are acceptable. Six weeks of silence is not.

**6. Move the Demo date to 2026-10-13, and hold 2026-10-23 for Product only if
the Demo lands on the 13th.** Pick the date from the arithmetic in §3, not from
the wish.

**7. Split the pull requests.** One change, one PR, two days maximum. PR #4
waited two days for review because it was 9,587 lines.

**8. Do not cut these**, whatever the pressure: tenancy and forced RLS, the
`entity` level, `int64` money, append-only classifications, balance
reconciliation, internal-transfer detection, and drill-down.

---

## 8. The risk in the other direction

I may be too pessimistic on velocity. The 2.12x overrun comes from changes 0.1
and 0.2, which are foundation work. Changes 1.1, 1.2 and 3.1 landed in
approximately four working days against a 6-day budget — that is **0.7x**, not
2.12x. [Likely — but the sample is two productive days and it excludes the
unbudgeted `eval/` work, so the true figure is worse than 0.7x]

If the team holds the 8 September rate, the Demo lands close to 1 October.

I am also possibly too hard on the web change. The token layer, the two palettes
and `check-web-tokens.sh` are the kind of foundation that stops a visual system
from decaying. That work will not need to be repeated.

**What does not change under either reading:** Track A has 10.5 days of work and
no owner, D-2 is one seventh closed, and nobody provisions the cluster. Those
three are true at every velocity.
