// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: readiness.sql

package db

import (
	"context"
)

const appliedMigrationVersion = `-- name: AppliedMigrationVersion :one
SELECT version_id
FROM goose_db_version
ORDER BY id DESC
LIMIT 1
`

// The highest version goose has recorded as applied. /readyz compares this
// against the binary's required version, derived from its embedded
// migrations (design D6/Q5), and fails naming both when the schema is
// behind.
func (q *Queries) AppliedMigrationVersion(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, appliedMigrationVersion)
	var version_id int64
	err := row.Scan(&version_id)
	return version_id, err
}
