// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: identity.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeAuthFlow = `-- name: ConsumeAuthFlow :one
DELETE FROM auth_flows
WHERE state = $1 AND expires_at > now()
RETURNING id, nonce, code_verifier
`

type ConsumeAuthFlowRow struct {
	ID           pgtype.UUID
	Nonce        string
	CodeVerifier string
}

// Consuming deletes, in one statement, so a replayed callback cannot race a
// first one: exactly one DELETE can return a row. A callback whose state
// matches nothing and one whose flow was already consumed are therefore
// indistinguishable, which is deliberate -- the caller learns nothing from
// the difference.
func (q *Queries) ConsumeAuthFlow(ctx context.Context, state string) (ConsumeAuthFlowRow, error) {
	row := q.db.QueryRow(ctx, consumeAuthFlow, state)
	var i ConsumeAuthFlowRow
	err := row.Scan(&i.ID, &i.Nonce, &i.CodeVerifier)
	return i, err
}

const deleteExpiredAuthFlows = `-- name: DeleteExpiredAuthFlows :execrows
DELETE FROM auth_flows WHERE expires_at < now()
`

func (q *Queries) DeleteExpiredAuthFlows(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, deleteExpiredAuthFlows)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteExpiredSessions = `-- name: DeleteExpiredSessions :execrows
DELETE FROM sessions WHERE expires_at < $1
`

// Sessions outlive their expiry by a retention window so a recently expired
// session is still visible to an operator asking what happened.
func (q *Queries) DeleteExpiredSessions(ctx context.Context, expiresAt pgtype.Timestamptz) (int64, error) {
	result, err := q.db.Exec(ctx, deleteExpiredSessions, expiresAt)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const findIdentity = `-- name: FindIdentity :one

SELECT provider, subject, user_id, created_at
FROM user_identities
WHERE provider = $1 AND subject = $2
`

type FindIdentityParams struct {
	Provider string
	Subject  string
}

// Queries for the four identity tables: users, user_identities, sessions and
// auth_flows.
//
// These four tables are outside row-level security (deploy/db/rls-exempt-
// tables.txt), so nothing at the database level filters what they return.
// Every statement in this file therefore reads by primary key or unique key,
// and joins only to another of the four. scripts/check-identity-queries.sh
// enforces both properties: no other query file may name these tables, and
// nothing here may reach outside them. See openspec add-identity design 1.1.
//
// There is deliberately no update-user statement and no update-identity
// statement. The account record is provisioned once and never written by a
// later token claim (design D3a); the identity binding is insert-once, and
// vekst_app holds no UPDATE privilege on it at all (design D3b).
// The whole of sign-in resolution. An identity is (provider, subject) and
// never an email: an issuer may reuse an email across different end users
// over time, so matching a login by address is the standard account-takeover
// path.
func (q *Queries) FindIdentity(ctx context.Context, arg FindIdentityParams) (UserIdentity, error) {
	row := q.db.QueryRow(ctx, findIdentity, arg.Provider, arg.Subject)
	var i UserIdentity
	err := row.Scan(
		&i.Provider,
		&i.Subject,
		&i.UserID,
		&i.CreatedAt,
	)
	return i, err
}

const findLiveSessionWithUser = `-- name: FindLiveSessionWithUser :one
SELECT
    s.id           AS session_id,
    s.expires_at   AS session_expires_at,
    u.id           AS user_id,
    u.email        AS user_email,
    u.name         AS user_name,
    u.locale       AS user_locale
FROM sessions s
JOIN users u ON u.id = s.user_id
WHERE s.token_sha256 = $1
  AND s.revoked_at IS NULL
  AND s.expires_at > now()
`

type FindLiveSessionWithUserRow struct {
	SessionID        pgtype.UUID
	SessionExpiresAt pgtype.Timestamptz
	UserID           pgtype.UUID
	UserEmail        pgtype.Text
	UserName         pgtype.Text
	UserLocale       string
}

// The middleware's one read per request: a unique-index probe on the token
// hash, joined to the session's own user. Both tables are inside the exempt
// four, so this join stays within the boundary the narrowness check draws.
// Expiry and revocation are predicates here rather than checks in Go, so a
// revoked or expired session simply matches nothing.
func (q *Queries) FindLiveSessionWithUser(ctx context.Context, tokenSha256 []byte) (FindLiveSessionWithUserRow, error) {
	row := q.db.QueryRow(ctx, findLiveSessionWithUser, tokenSha256)
	var i FindLiveSessionWithUserRow
	err := row.Scan(
		&i.SessionID,
		&i.SessionExpiresAt,
		&i.UserID,
		&i.UserEmail,
		&i.UserName,
		&i.UserLocale,
	)
	return i, err
}

const findUserByID = `-- name: FindUserByID :one
SELECT id, email, name, locale, created_at
FROM users
WHERE id = $1
`

func (q *Queries) FindUserByID(ctx context.Context, id pgtype.UUID) (User, error) {
	row := q.db.QueryRow(ctx, findUserByID, id)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Locale,
		&i.CreatedAt,
	)
	return i, err
}

const insertAuthFlow = `-- name: InsertAuthFlow :one
INSERT INTO auth_flows (state, nonce, code_verifier, expires_at)
VALUES ($1, $2, $3, $4)
RETURNING id
`

type InsertAuthFlowParams struct {
	State        string
	Nonce        string
	CodeVerifier string
	ExpiresAt    pgtype.Timestamptz
}

func (q *Queries) InsertAuthFlow(ctx context.Context, arg InsertAuthFlowParams) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, insertAuthFlow,
		arg.State,
		arg.Nonce,
		arg.CodeVerifier,
		arg.ExpiresAt,
	)
	var id pgtype.UUID
	err := row.Scan(&id)
	return id, err
}

const insertIdentity = `-- name: InsertIdentity :exec
INSERT INTO user_identities (provider, subject, user_id)
VALUES ($1, $2, $3)
`

type InsertIdentityParams struct {
	Provider string
	Subject  string
	UserID   pgtype.UUID
}

// Insert-once, with no ON CONFLICT clause. Repointing a subject at another
// user is account takeover; vekst_app has no UPDATE privilege here, so this
// cannot become an upsert without a migration that CODEOWNERS would catch.
func (q *Queries) InsertIdentity(ctx context.Context, arg InsertIdentityParams) error {
	_, err := q.db.Exec(ctx, insertIdentity, arg.Provider, arg.Subject, arg.UserID)
	return err
}

const insertSession = `-- name: InsertSession :one
INSERT INTO sessions (token_sha256, user_id, expires_at, user_agent)
VALUES ($1, $2, $3, $4)
RETURNING id, user_id, created_at, expires_at
`

type InsertSessionParams struct {
	TokenSha256 []byte
	UserID      pgtype.UUID
	ExpiresAt   pgtype.Timestamptz
	UserAgent   pgtype.Text
}

type InsertSessionRow struct {
	ID        pgtype.UUID
	UserID    pgtype.UUID
	CreatedAt pgtype.Timestamptz
	ExpiresAt pgtype.Timestamptz
}

// token_sha256 is the SHA-256 of 32 random bytes; the raw value lives only in
// the cookie and is never stored, so a read of this table yields nothing a
// browser could present.
func (q *Queries) InsertSession(ctx context.Context, arg InsertSessionParams) (InsertSessionRow, error) {
	row := q.db.QueryRow(ctx, insertSession,
		arg.TokenSha256,
		arg.UserID,
		arg.ExpiresAt,
		arg.UserAgent,
	)
	var i InsertSessionRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.CreatedAt,
		&i.ExpiresAt,
	)
	return i, err
}

const insertUser = `-- name: InsertUser :one
INSERT INTO users (email, name, locale)
VALUES ($1, $2, $3)
RETURNING id, email, name, locale, created_at
`

type InsertUserParams struct {
	Email  pgtype.Text
	Name   pgtype.Text
	Locale string
}

// Provisioning, and the only statement that ever writes a user row. A
// duplicate address raises 23505 on users_email_key, which the caller
// translates into the email_taken code -- the constraint is the check, so two
// concurrent first sign-ins on one address cannot both succeed.
func (q *Queries) InsertUser(ctx context.Context, arg InsertUserParams) (User, error) {
	row := q.db.QueryRow(ctx, insertUser, arg.Email, arg.Name, arg.Locale)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Locale,
		&i.CreatedAt,
	)
	return i, err
}

const revokeSession = `-- name: RevokeSession :exec
UPDATE sessions SET revoked_at = now()
WHERE token_sha256 = $1 AND revoked_at IS NULL
`

// Sign-out marks the row revoked rather than deleting it, so the session is
// auditable afterwards and the expiry job is what finally removes it.
func (q *Queries) RevokeSession(ctx context.Context, tokenSha256 []byte) error {
	_, err := q.db.Exec(ctx, revokeSession, tokenSha256)
	return err
}

const touchSession = `-- name: TouchSession :exec
UPDATE sessions SET last_seen_at = now() WHERE id = $1
`

func (q *Queries) TouchSession(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, touchSession, id)
	return err
}
