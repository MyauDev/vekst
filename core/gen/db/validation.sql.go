// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: validation.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getValidationForBatch = `-- name: GetValidationForBatch :one
SELECT id, org_id, batch_id, outcome, row_count, error_count, warning_count,
       balance_check_passed, report_jsonb,
       overridden_by, override_reason, overridden_at, created_at
FROM import_validations
WHERE batch_id = $1
`

// batch_id alone, with no org_id beside it -- the row is keyed (org_id, id)
// and the policy supplies the org_id half, the same shape as ingest.sql's
// GetImportBatch.
func (q *Queries) GetValidationForBatch(ctx context.Context, batchID pgtype.UUID) (ImportValidation, error) {
	row := q.db.QueryRow(ctx, getValidationForBatch, batchID)
	var i ImportValidation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.BatchID,
		&i.Outcome,
		&i.RowCount,
		&i.ErrorCount,
		&i.WarningCount,
		&i.BalanceCheckPassed,
		&i.ReportJsonb,
		&i.OverriddenBy,
		&i.OverrideReason,
		&i.OverriddenAt,
		&i.CreatedAt,
	)
	return i, err
}

const insertValidation = `-- name: InsertValidation :one

INSERT INTO import_validations (
    org_id, batch_id, outcome, row_count, error_count, warning_count,
    balance_check_passed, report_jsonb
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8
)
RETURNING id, org_id, batch_id, outcome, row_count, error_count, warning_count,
          balance_check_passed, report_jsonb,
          overridden_by, override_reason, overridden_at, created_at
`

type InsertValidationParams struct {
	OrgID              pgtype.UUID
	BatchID            pgtype.UUID
	Outcome            string
	RowCount           int32
	ErrorCount         int32
	WarningCount       int32
	BalanceCheckPassed pgtype.Bool
	ReportJsonb        []byte
}

// Queries for import_validations (change 2.3, migration 010).
//
// No statement here carries a `WHERE org_id = ...` predicate -- the same
// reason as every other file in this directory: the table's FORCE'd
// row-level-security policy already restricts every statement to
// app_current_org(), and db.InTx sets that context before any of these run.
// INSERT is the exception, the same way it is in ingest.sql: org_id is bound
// explicitly because the row does not exist yet for a policy to restrict.
//
// RecordOverride never lists outcome, row_count, error_count, warning_count,
// balance_check_passed or report_jsonb among the columns it sets. Migration
// 010 revokes table-level UPDATE and grants back only the three override
// columns, so a query naming any of the six would not even compile against
// that grant.
func (q *Queries) InsertValidation(ctx context.Context, arg InsertValidationParams) (ImportValidation, error) {
	row := q.db.QueryRow(ctx, insertValidation,
		arg.OrgID,
		arg.BatchID,
		arg.Outcome,
		arg.RowCount,
		arg.ErrorCount,
		arg.WarningCount,
		arg.BalanceCheckPassed,
		arg.ReportJsonb,
	)
	var i ImportValidation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.BatchID,
		&i.Outcome,
		&i.RowCount,
		&i.ErrorCount,
		&i.WarningCount,
		&i.BalanceCheckPassed,
		&i.ReportJsonb,
		&i.OverriddenBy,
		&i.OverrideReason,
		&i.OverriddenAt,
		&i.CreatedAt,
	)
	return i, err
}

const overriddenBatchesForPeriod = `-- name: OverriddenBatchesForPeriod :many
SELECT b.id, v.override_reason, v.overridden_at, v.balance_check_passed
FROM import_validations v
JOIN import_batches b ON b.id = v.batch_id
JOIN transactions t   ON t.batch_id = b.id
WHERE v.overridden_at IS NOT NULL
  AND t.entity_id = $1 AND t.booked_on >= $2 AND t.booked_on < $3
GROUP BY b.id, v.override_reason, v.overridden_at, v.balance_check_passed
`

type OverriddenBatchesForPeriodParams struct {
	EntityID   pgtype.UUID
	BookedOn   pgtype.Date
	BookedOn_2 pgtype.Date
}

type OverriddenBatchesForPeriodRow struct {
	ID                 pgtype.UUID
	OverrideReason     pgtype.Text
	OverriddenAt       pgtype.Timestamptz
	BalanceCheckPassed pgtype.Bool
}

// Change 4.1 calls this and puts what it returns on the report (design D4).
// A report that does not call it is a report that hides an override, so
// 4.1's own task list carries a test that fails when the call is missing.
// Unused here -- this change has no report to put it on yet.
func (q *Queries) OverriddenBatchesForPeriod(ctx context.Context, arg OverriddenBatchesForPeriodParams) ([]OverriddenBatchesForPeriodRow, error) {
	rows, err := q.db.Query(ctx, overriddenBatchesForPeriod, arg.EntityID, arg.BookedOn, arg.BookedOn_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OverriddenBatchesForPeriodRow
	for rows.Next() {
		var i OverriddenBatchesForPeriodRow
		if err := rows.Scan(
			&i.ID,
			&i.OverrideReason,
			&i.OverriddenAt,
			&i.BalanceCheckPassed,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const recordOverride = `-- name: RecordOverride :execrows
UPDATE import_validations
SET overridden_by = $2, override_reason = $3, overridden_at = now()
WHERE batch_id = $1
`

type RecordOverrideParams struct {
	BatchID        pgtype.UUID
	OverriddenBy   pgtype.UUID
	OverrideReason pgtype.Text
}

// Written once (design D4): nothing here reads the current row first to
// decide whether to write, because there is no legitimate second write --
// overridden_at IS NULL is not checked here because the CHECK constraint
// import_validations_override_only_over_warnings and the handler's own
// lookup (task 5.3) are what refuse a second override, not this statement's
// WHERE clause.
func (q *Queries) RecordOverride(ctx context.Context, arg RecordOverrideParams) (int64, error) {
	result, err := q.db.Exec(ctx, recordOverride, arg.BatchID, arg.OverriddenBy, arg.OverrideReason)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
