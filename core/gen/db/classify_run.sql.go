// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: classify_run.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const finishClassificationRun = `-- name: FinishClassificationRun :one
UPDATE classification_runs
   SET status       = $2,
       failure_code = $3,
       finished_at  = now()
 WHERE batch_id = $1 AND status = 'running'
RETURNING org_id, id, batch_id, status, failure_code, chunk_count, classified_count, review_count, started_at, finished_at
`

type FinishClassificationRunParams struct {
	BatchID     pgtype.UUID
	Status      string
	FailureCode pgtype.Text
}

// The run is over, one way or the other.
//
// `failure_code` is NULL on success and set on failure, which is what the
// table's own CHECK requires: a failure names its reason and a success has none.
// The WHERE clause makes a second finish affect no row, which db.ExactlyOneRow
// turns into an error rather than a silent success -- a run finished twice
// would mean the job ran twice, and that is worth hearing about.
func (q *Queries) FinishClassificationRun(ctx context.Context, arg FinishClassificationRunParams) (ClassificationRun, error) {
	row := q.db.QueryRow(ctx, finishClassificationRun, arg.BatchID, arg.Status, arg.FailureCode)
	var i ClassificationRun
	err := row.Scan(
		&i.OrgID,
		&i.ID,
		&i.BatchID,
		&i.Status,
		&i.FailureCode,
		&i.ChunkCount,
		&i.ClassifiedCount,
		&i.ReviewCount,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}

const getClassificationRun = `-- name: GetClassificationRun :one
SELECT org_id, id, batch_id, status, failure_code, chunk_count, classified_count, review_count, started_at, finished_at FROM classification_runs WHERE batch_id = $1
`

// What the import screen reads beside the batch's own status. Absent is a real
// answer -- a batch that has not been classified yet has no run -- and is not
// the same as a run that failed.
func (q *Queries) GetClassificationRun(ctx context.Context, batchID pgtype.UUID) (ClassificationRun, error) {
	row := q.db.QueryRow(ctx, getClassificationRun, batchID)
	var i ClassificationRun
	err := row.Scan(
		&i.OrgID,
		&i.ID,
		&i.BatchID,
		&i.Status,
		&i.FailureCode,
		&i.ChunkCount,
		&i.ClassifiedCount,
		&i.ReviewCount,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}

const insertClassificationRun = `-- name: InsertClassificationRun :one

INSERT INTO classification_runs (org_id, batch_id)
VALUES (app_current_org(), $1)
RETURNING org_id, id, batch_id, status, failure_code, chunk_count, classified_count, review_count, started_at, finished_at
`

// The record of classifying one batch. Change 3.4, capability
// `classification-run`.
//
// Every statement runs inside db.InTx, which sets the tenant context, and none
// of them restates the tenant predicate: row-level security already admits this
// organisation's rows and nothing else. Each takes `org_id` from
// `app_current_org()` rather than as a parameter, for the same reason -- a
// parameter is a chance to pass the wrong value, and the transaction already
// knows the right one.
// Opened the moment the job starts, with everything at zero.
//
// No "does a run already exist" read first. `UNIQUE (org_id, batch_id)` is the
// guard, and it is the only one that holds when two enqueues of the same batch
// race: a check-then-insert has a window between the two halves, and the window
// is exactly where a duplicate job lands. The loser gets 23505 and stops, which
// is what idempotent means here.
func (q *Queries) InsertClassificationRun(ctx context.Context, batchID pgtype.UUID) (ClassificationRun, error) {
	row := q.db.QueryRow(ctx, insertClassificationRun, batchID)
	var i ClassificationRun
	err := row.Scan(
		&i.OrgID,
		&i.ID,
		&i.BatchID,
		&i.Status,
		&i.FailureCode,
		&i.ChunkCount,
		&i.ClassifiedCount,
		&i.ReviewCount,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}

const recordChunkResult = `-- name: RecordChunkResult :one
UPDATE classification_runs
   SET chunk_count      = chunk_count + 1,
       classified_count = classified_count + $2,
       review_count     = review_count + $3
 WHERE batch_id = $1 AND status = 'running'
RETURNING org_id, id, batch_id, status, failure_code, chunk_count, classified_count, review_count, started_at, finished_at
`

type RecordChunkResultParams struct {
	BatchID         pgtype.UUID
	ClassifiedCount int32
	ReviewCount     int32
}

// One chunk's outcome, added to the run in the same transaction that wrote the
// chunk's classifications.
//
// Increments rather than assignments: the counters are the sum of what the
// chunks did, and a worker that computed a running total in Go and wrote it
// back would lose the arithmetic of any chunk whose transaction rolled back
// after it. Read-modify-write over a whole-run total would also be a lost
// update the moment anything else touches the row.
func (q *Queries) RecordChunkResult(ctx context.Context, arg RecordChunkResultParams) (ClassificationRun, error) {
	row := q.db.QueryRow(ctx, recordChunkResult, arg.BatchID, arg.ClassifiedCount, arg.ReviewCount)
	var i ClassificationRun
	err := row.Scan(
		&i.OrgID,
		&i.ID,
		&i.BatchID,
		&i.Status,
		&i.FailureCode,
		&i.ChunkCount,
		&i.ClassifiedCount,
		&i.ReviewCount,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}
