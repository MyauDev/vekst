# Ownership follows the two tracks in docs/IMPLEMENTATION_PLAN.md section 2.1.
# The rule it encodes: after the foundation, do not both edit the same file.
#
# Both tracks are staffed by the same two people (@MyauDev/admin), so listing
# it once per line is what "review from both owners" resolves to in
# CODEOWNERS terms -- a second listing of the same team is not a second
# reviewer. Where a line below says "both reviewers required," that intent is
# enforced by the repository's branch-protection required-approvals count,
# not by this file: CODEOWNERS can name which team owns a path, but it cannot
# require two specific people's sign-off within one team on its own.

# Default: no owner. Anything unclaimed is reviewed by whoever is around.

# --- Track A: ingest -------------------------------------------------------
/core/internal/ingest/      @MyauDev/admin
/core/internal/dedup/       @MyauDev/admin

# --- Track B: meaning ------------------------------------------------------
/core/classify/              @MyauDev/admin
/core/internal/report/       @MyauDev/admin
/classifier/                 @MyauDev/admin

# --- Both reviewers required (branch protection enforces the count) -------
# Contracts and deployment shape. A change here reaches every service.
/proto/                      @MyauDev/admin
/deploy/k8s/base/            @MyauDev/admin

# The RLS-exemption allowlist (openspec design D4, change 0.2). Adding a
# table here is a security decision: it says no tenant's data can ever land
# in it.
/deploy/db/rls-exempt-tables.txt   @MyauDev/admin

# Change 0.2 adds /core/internal/db and the money packages here, when those
# paths exist. GitHub silently ignores a CODEOWNERS entry for a path that does
# not exist, so listing them early would look enforced while doing nothing.
